steps:
  mtaBuild:
    buildTarget: 'CF'
    mtaJarLocation: '/opt/sap/mta/lib/mta.jar'
   cloudFoundryDeploy:
    deployTool: 'mtaDeployPlugin'
    cloudFoundry:
      org: 'testOrg'
      space: 'testSpace'
      credentialsId: 'NEO_DEPLOY'