@Library('piper-lib-os') _


node(){
  stage('Prepare')   {
      deleteDir()
      checkout scm
      setupCommonPipelineEnvironment script:this
  }

  stage('Build')   {
      withEnv(['M2_HOME=/opt/maven/apache-maven-3.6.3']) {
      mtaBuild script:this
             }
  }
}
